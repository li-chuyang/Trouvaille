# Calculator demo

This tiny project is an isolated coding task for the agent. `calculator.add` currently subtracts; the test expects addition. Run `uv run python demo/run_demo.py` from the main project directory to copy this fixture into a temporary Git workspace and let the agent repair it.
